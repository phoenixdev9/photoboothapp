<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- Button background color -->
    <stroke
        android:width="2dp"
        android:color="@android:color/transparent"/> <!-- Border color and width -->
    <corners android:radius="4dp"/> <!-- Optional: To make the corners slightly rounded -->
</shape>